language: node_js
node_js:
- node
dist: xenial
cache:
- npm
install: npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
  verbose: true
